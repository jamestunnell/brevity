module Brevity

grammar Sequence
  include Note
  include DynamicLevel
  include DynamicChange
  
  rule sequence
    start_dyn:(dynamic_level [\s]+)? first_note:note more:([\s]+ dyn_change:(dynamic_change [\s]+)? note)* end_dyn_change:([\s]+ dynamic_change)? <SequenceNode>
  end
end

end
